@model BMG.Models.AspNetUser

@foreach (var i in Model.Photos)
{
    if (i.AspNetUser != null&&i.Traveling==null && i.Main == true)
    {
        <img src="https://@Html.DisplayFor(model => i.Path)" alt="@Html.DisplayFor(model => i.Description)" width="100%"/>
    }
}