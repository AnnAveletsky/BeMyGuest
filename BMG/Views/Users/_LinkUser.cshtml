@model BMG.Models.AspNetUser
@if (Model.FirstName == null && Model.SecondName == null)
{
    @Html.ActionLink(Model.UserName, "/../Users/Details", new { id = Model.Id })
}
else
{
    @Html.ActionLink(Model.FirstName + " " + Model.SecondName, "/../Users/Details", new { id = Model.Id })
}